/**
 * Game constants — single source of truth for values that change with game updates.
 * When 2K patches the game (level cap increase, new vault hunter, etc.), update HERE.
 */

/** Maximum character/item level. Updated with each level cap increase. */
export const MAX_LEVEL = 60;

/** Default level for new items in builders. */
export const DEFAULT_LEVEL = 60;
