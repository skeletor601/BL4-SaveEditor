# BL4 AIO API - copy to .env and adjust
PORT=3001
NODE_ENV=development

# Optional: parts data source URL for /admin/update (e.g. ZIP or JSON endpoint)
PARTS_SOURCE_URL=

# Optional: secret for /admin/update (leave empty to disable endpoint in production)
ADMIN_SECRET=
